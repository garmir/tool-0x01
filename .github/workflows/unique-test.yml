name: Unique Test - tool-0x01
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - tool-0x01
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: tool-0x01 ===" > unique-test-tool-0x01.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-tool-0x01.txt
            echo "Test ID: TEST_004" >> unique-test-tool-0x01.txt
            echo "Timestamp: $(date)" >> unique-test-tool-0x01.txt
            curl -s ifconfig.me >> unique-test-tool-0x01.txt
            echo "Unique instruction: Tool development and compilation testing" >> unique-test-tool-0x01.txt
          '

      - name: Ubuntu Fallback - tool-0x01
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for tool-0x01" > fallback-tool-0x01.txt
          curl -s ifconfig.me >> fallback-tool-0x01.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-tool-0x01
          path: "*.txt"
          retention-days: 2